import { GenericObject } from "src/types/app";

const colors: GenericObject = {
  primary: "#1cf769",
  shadow: "#1cf769",
  secondary: "#4c4b47"
};

const font: GenericObject = {
  family: "monospace",
  size: "1rem",
  buttonSize: "700"
};

export default { colors, font };
